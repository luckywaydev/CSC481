import type { NextConfig } from "next";

// ตั้งค่า Next.js ตามที่เรียนมา
const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true, // ใช้ React Compiler แบบใหม่
};

export default nextConfig;
