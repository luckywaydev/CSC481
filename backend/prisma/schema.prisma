// Prisma Schema - ตั้งค่าพื้นฐาน
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ตอนนี้ยังไม่มี model แค่ setup connection ก่อน
